# cache_delete, if the cache doesn't exist throws a warning

    `cache_dir` doesn't exist: 
    * inexistent

# cache_delete, if the cache exists throws a warning

    `cache_dir` doesn't exist: 
    * inexistent

